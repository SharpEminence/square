
      <!-- partial:partials/_navbar.html -->
      <nav class="adminMainHeader navbar default-layout col-lg-12 col-12 p-0 fixed-top d-flex flex-row">
        <div class="adminLogo text-center navbar-brand-wrapper d-flex align-items-top justify-content-center">
          <a class="navbar-brand brand-logo" href="/">
            <img src="/assets/images/adminLogo.png" alt="logo" /> </a>
          <a class="navbar-brand brand-logo-mini" href="/">
            <img src="/assets/images/logo-mini.svg" alt="logo" /> </a>
        </div>
        <div class="adimnHeader navbar-menu-wrapper d-flex align-items-center">
            <span class="leftSideBarColl">
                <button class="openbtn" onclick="openNav()"><img src="../assets/images/navIcon/toggleBar.png" /></button>
            </span>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown d-none d-xl-inline-block user-dropdown">
              <a class="nav-link dropdown-toggle" id="UserDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
                  <span class="adminName">
                    <% if(typeof user !== "undefined" && user.role == 1) {%>
                      Admin
                    <% } else{%>
                      Sub Admin
                      <% } %>
                  
                  </span>
                <img class="img-xs rounded-circle" src='/uploads/<%=user.profile_img %>' alt="Profile image"> </a>
              <div class="dropdown-menu dropdown-menu-right navbar-dropdown" aria-labelledby="UserDropdown">
                <div class="dropdown-header text-center">
                  <img class="img-md rounded-circle" src='/uploads/<%=user.profile_img %>' alt="Profile image">
                  <p class="mb-1 mt-3 font-weight-semibold">
                    <%= (typeof user !== "undefined") ? user.fullname : "" %>
                  </p>
                  <p class="font-weight-light text-muted mb-0">
                    <%= (typeof user !== "undefined") ? user.email : "" %>
                  </p>
                </div>
                <a href="/edit-admin" class="dropdown-item">My Profile <i class="dropdown-item-icon ti-dashboard"></i></a>
                <a href="/change-password" class="dropdown-item">Change Password<i class="dropdown-item-icon ti-comment-alt"></i></a>
                <!-- <a class="dropdown-item">Activity<i class="dropdown-item-icon ti-location-arrow"></i></a>
                <a class="dropdown-item">FAQ<i class="dropdown-item-icon ti-help-alt"></i></a> -->
                <a href="/logout" class="dropdown-item">Sign Out<i class="dropdown-item-icon ti-power-off"></i></a>
              </div>
            </li>
          </ul>
          <button class="navbar-toggler navbar-toggler-right d-lg-none align-self-center" type="button" data-toggle="offcanvas">
            <span class="mdi mdi-menu"></span>
          </button>
        </div>
      </nav>
